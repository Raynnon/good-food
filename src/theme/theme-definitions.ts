export interface FontSizes {
  p: number;
  h5: number;
  h4: number;
  h3: number;
  h2: number;
  h1: number;
  title: number;
}

export interface Fonts {
  body: string;
  heading: string;
}

export interface Colors {
  primary: string;
  darkGrey: string;
  lightGrey: string;
  white: string;
}
